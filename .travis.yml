language: node_js
install:
- npm install -g gulp
- npm install
- chmod ugo+x ./build.sh

script:
- ./build.sh

branches:
  only:
    - master
env:
  global:
    - GH_REPO: https://github.com/zpbappi/angular-autowrap.git